<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Hide the status bar -->
        <item name="android:windowFullscreen">true</item>
        <!-- Skip showing the application title -->
        <item name="android:windowNoTitle">true</item>
        <!--We will be using the toolbar so no need to show ActionBar-->
        <item name="android:windowActionBar">false</item>
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">#3F51B5</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryVariant">#303F9F</item>
        <item name="colorPrimaryDark">?colorPrimary</item>

        <item name="android:textColorPrimary">?android:colorForeground</item>
        <item name="android:textAppearance">@style/AppTheme.TextAppearance</item>
        <item name="android:textAppearanceSmall">@style/AppTheme.TextAppearance</item>
    </style>

    <style name="AppTheme.TextAppearance" parent="@android:style/TextAppearance.Material">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">?android:textColorPrimary</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:paddingEnd">0dp</item>
    </style>

    <style name="LibraryTitle" parent="@android:style/TextAppearance.Material.Widget.ActionBar.Title">
        <item name="android:textColor">?colorOnPrimary</item>
    </style>

    <style name="LibraryTitle.Inverse" parent="LibraryTitle">
        <item name="android:background">?android:colorBackground</item>
        <item name="android:textColor">?android:colorForeground</item>
    </style>
</resources>